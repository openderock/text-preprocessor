/**
 * Normalizing texts before any natural language processing
 * @typedef {{name: String, workClosure: () => (Error, Date) => void}} Options
 * @param {String} text 
 * @param {Options} options 
 */
function preprocessor(text, options) {

}

module.export = preprocessor;